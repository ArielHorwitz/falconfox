---
name: falconfox-pointer
description: Resolve a spoken FalconFox session name and move the Telegram focus pointer.
---

For every request, run `falconfox list`, resolve the requested session by id or
name, and write only its exact session id plus a newline to the pointer file
named in `AGENTS.md`. Never do project work in this session.
